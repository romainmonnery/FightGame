package fightgame.model;

public class Dimension {

	//Attributes which corresponds to the two caracteristics of a dimension : the width and the height
	/*private int width;
	private int height;
	
	//Constructor which builds dimensions with the two attributes entered in parameters
	public Dimension(int width, int height) {
		this.width=width;
		this.height=height;
	}
	
	//Constructor which builds dimension with the attributes declared in the class 
	public Dimension(Dimension dimension) {
		this(dimension.width, dimension.height);
	}
	
	//Getters
	//Getting the width
	public int getWidth() {
		return this.width;
	}
	
	//Getting the height
	public int getHeight() {
		return this.height;
	}*/
}
